# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright
test-results
playwright-report
playwright/.cache

# Dev server log (.claude/skills/dev-server/start.sh)
.dev-server.log

# ts-scratch run dirs are normally self-cleaning (try/finally), this is
# belt-and-suspenders for an interrupted run (.claude/skills/ts-scratch/)
.claude/skills/ts-scratch/.tmp/

# Claude Code local runtime state (personal to this machine/session, not
# shared) — .claude/skills/ itself IS committed, these specific files aren't
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
