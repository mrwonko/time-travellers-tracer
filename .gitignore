# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright (once tests are added — see memory: playwright-testing-plan)
test-results
playwright-report
playwright/.cache

# Dev server log (.claude/skills/dev-server/start.sh)
.dev-server.log

# Claude Code local runtime state (personal to this machine/session, not
# shared) — .claude/skills/ itself IS committed, these specific files aren't
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
